# Server
NODE_ENV=development
PORT=8080

# CORS
# Comma-separated list of allowed origins (no trailing slash).
# Supports a single '*' wildcard per entry, e.g. https://*.vercel.app
CORS_ORIGINS=http://localhost:5173,https://hz-it-company-fny3.vercel.app

# Postgres
# Local example:
# DATABASE_URL=postgres://user:password@localhost:5432/hzit

# Supabase (recommended): Session Pooler (IPv4 compatible)
# Use the connection string from Supabase "Session pooler".
# NOTE: If your password contains special characters (e.g. @, #, :, /), URL-encode it.
# Example: @ -> %40
# Port is usually 6543 for pooler.
DATABASE_URL=postgresql://postgres.<project-ref>:<url-encoded-password>@aws-<region>.pooler.supabase.com:6543/postgres

# Dev helper: if true, the server will run db/schema.sql on startup when core tables are missing.
# Keep this false in production unless you explicitly want the API to run DDL.
DB_AUTO_SCHEMA=true

# Auth
# Use a long random string (>=32 chars)
JWT_SECRET=change-me-to-a-long-random-string

# Admin
# Optional: if set, this email will receive admin role on OTP login.
ADMIN_EMAIL=hzitcompany@gmail.com
# Bcrypt hash for the admin password (generate locally; never store plaintext passwords).
# Example: $2a$12$...
ADMIN_PASSWORD_HASH=

# Rate limiting (optional)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=20

# Email (Resend)
# Leave blank to disable email sending.
RESEND_API_KEY=
MAIL_FROM=
MAIL_TO=hzitcompany@gmail.com

# OTP (Supabase Auth)
# Required for phone OTP in production.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# OTP expiry (UI hint only; Supabase controls OTP rules)
OTP_EXPIRES_SECONDS=300

# Payments (Razorpay) - optional until you enable checkout
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Supabase Storage (Resumes)
# Create a private bucket named "resumes" in Supabase Storage.
# Use your project URL and SERVICE_ROLE key (server-side only).
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=resumes
# Upload size limit (bytes). Default is 5MB.
CAREER_UPLOAD_MAX_BYTES=5242880

# Optional SMTP settings (only needed if you later switch back to SMTP)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
