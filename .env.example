# Frontend (Vite) API base URL
# Create a local .env (ignored by git) or set this in your environment.
VITE_API_BASE_URL=http://localhost:8080

# Frontend (Vite) Supabase
# Used for client-side Supabase features (if enabled).
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key

# Admin email (used client-side for admin guard)
# Google Identity Services (GIS) – shows the Google login button
VITE_GOOGLE_CLIENT_ID=

# Admin email (users with this email get admin role on OTP/Google login)
VITE_ADMIN_EMAIL=hzitcompany@gmail.com
